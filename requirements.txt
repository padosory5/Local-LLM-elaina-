# Local language model
ollama>=0.6,<1

# Speech recognition and voice activity detection
faster-whisper>=1.1,<2
silero-vad>=6,<7
sounddevice>=0.5,<1
torch>=2.5
numpy>=2,<3

# Text-to-speech and playback
elevenlabs>=2,<3
pygame>=2.6,<3

# Persistent semantic memory
faiss-cpu>=1.9,<2
sentence-transformers>=5,<6
SQLAlchemy>=2,<3

# Screen capture and image understanding
mss>=10,<11
Pillow>=11,<13
google-cloud-vision>=3.10,<4
google-api-python-client>=2,<3
google-auth-oauthlib>=1.2,<2
google-auth-httplib2>=0.2,<1

# Search and integrations
ddgs>=9,<10
mcp>=1.20,<2
pydantic>=2,<3
websockets>=15,<17

# Configuration
PyYAML>=6,<7
python-dotenv>=1,<2
tzdata>=2025

# Desktop control (Windows only) -- Phase 4A app/process/file control,
# Phase 4B.1 read-only UI Automation window/control observation, and the
# Phase 4E screen-native browser driver, which reads the live page of an
# already-open browser through UI Automation and drives the real mouse and
# keyboard through the Win32 SendInput API. No extra dependency: the
# screen driver needs exactly these two packages.
pywin32>=306
pywinauto>=0.6,<1

# Phase 4C browser-page control -- connects to Elaina's dedicated
# Chromium-based browser profile (for example, Whale) over the Chrome DevTools
# Protocol. Only the Python client is needed; no bundled browser download
# (`playwright install`), because Elaina launches the installed browser itself.
playwright>=1.62,<2
